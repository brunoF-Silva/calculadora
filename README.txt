git pull origin developer
git branch developer
git checkout developer
git pull origin developer


dados da tabela:

CREATE TABLE Calculos (
    ID INT PRIMARY KEY,
    numero1 DECIMAL,
    numero2 DECIMAL,
    operacao VARCHAR(50),
    data TIMESTAMP
);
